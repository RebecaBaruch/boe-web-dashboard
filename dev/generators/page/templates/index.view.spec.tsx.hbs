import React from 'react';
import { screen } from '@testing-library/react';
import {{pascalCase name}} from '../../../../../src/pages/{{kebabCase name}}/view/{{kebabCase name}}.view';
import { renderWithTheme } from '../../../../../src/utils/render-with-theme';

describe('{{pascalCase name}}', () => {
    it('should render {{pascalCase name}} with text {{pascalCase name}}', () => {
        const { container } = renderWithTheme(<{{pascalCase name}} />);

        expect(screen.getByText('{{pascalCase name}} - Generated page')).toBeInTheDocument();

        expect(container).toMatchSnapshot();
    });
});
